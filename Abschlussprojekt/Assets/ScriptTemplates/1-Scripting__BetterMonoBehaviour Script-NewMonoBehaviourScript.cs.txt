using UnityEngine;
using UnityEngine.InputSystem;

    #ROOTNAMESPACEBEGIN#
public class #SCRIPTNAME# : MonoBehaviour
{
    //Awake is calles once after the MonoBehaviour is instanciated
    void Awake()
    {  
        #NOTRIM#
    }

    //Coroutine template. Can be called with StartCoroutine(Coroutine)
    IEnumerator Coroutine()
    {
         yield return null;
    }

    // Update is called once per frame
    void Update()
    {
        #NOTRIM#
    }
}
#ROOTNAMESPACEEND#
